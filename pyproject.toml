[project]
name = "gletch"
version = "0.5.0"
description = "Python system-fetch utility for macOS inspired by nitch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
gletch = "main:main"

[dependency-groups]
dev = [
  "pyinstaller>=6.0",
]
